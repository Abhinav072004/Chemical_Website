[build]
  command = "npm ci && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20.19.0"

# SPA routing (if you use React Router or any client-side routes)
[[redirects]]
from = "/*"
to = "/index.html"
status = 200
